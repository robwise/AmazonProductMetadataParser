/**
 * A collection of classes used by instances of {@link amazon_product_metadata_parser.Parser} to
 * send parsed {@link amazon_product_metadata_parser.dto.ProductDTO} objects. This allows for
 * reusing the same {@code Parser} class while changing how the parsed data is used.
 */
package amazon_product_metadata_parser.output;
